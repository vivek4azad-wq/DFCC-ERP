import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'in.co.dfcc.erp',
  appName: 'DFCCIL ERP',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
